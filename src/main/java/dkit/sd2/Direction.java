package dkit.sd2;

// Enumeration (Enum)
// This creates a new data type called Direction that is allowed to have only two values, RIGHT and LEFT
//
// Declare an enum variable as follows:      Direction bugDirection;
// Assign values as follows:                 bugDirection = Direction.RIGHT;
// Test values as follows:                  if( bugDirection == Direction.LEFT) ....
public enum Direction { RIGHT, LEFT }
